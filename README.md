# basic_batch_annotate
script to annotate images in batches via html

## usage:
* run below command to generate `report.html`
`python render.py`
* use `report.html` to export final annotated info as csv file.
